
    export type RemoteKeys = 'REMOTE_ALIAS_IDENTIFIER/SportsbookPage';
    type PackageType<T> = T extends 'REMOTE_ALIAS_IDENTIFIER/SportsbookPage' ? typeof import('REMOTE_ALIAS_IDENTIFIER/SportsbookPage') :any;